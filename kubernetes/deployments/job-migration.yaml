apiVersion: batch/v1
kind: Job
metadata:
  name: job-migration
  namespace: default
spec:
  backoffLimit: 5
  template:
    spec:
      containers:
      - command:
        - python
        - manage.py
        - migrate
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              key: user
              name: postgres-credentials
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: postgres-credentials
        - name: POSTGRES_HOST
          value: postgres-service
        image: gcr.io/pipeline-tutorial-231718/pipeline:b4a6591b7780fa2764e785d7a380908952f21bfc
        name: django
      restartPolicy: Never
